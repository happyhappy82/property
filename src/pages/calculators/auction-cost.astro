---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="경매비용 계산기 - 부동산 트렌드 리뷰" description="부동산 경매 낙찰 시 필요한 총 투자금액을 계산하세요. 낙찰가를 입력하면 취득세, 명도비용, 수리비 등을 포함한 실제 투자금액을 확인할 수 있습니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">경매비용 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">낙찰가를 입력하여 경매 취득에 필요한 총 비용을 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">낙찰가</label>
          <div class="relative">
            <input type="text" id="bidPrice" placeholder="300,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">총 투자금액</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">총 투자 필요금액</span><span class="font-bold text-blue-700 dark:text-blue-300" id="totalInvestment"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">제비용 합계</span><span class="font-medium dark:text-gray-200" id="totalIncidentalCost"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">비용 세부 내역</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">낙찰가</span><span class="dark:text-gray-200" id="bidPriceResult"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">취득세 (4.6%)</span><span class="dark:text-gray-200" id="acquisitionTax"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">명도비용</span><span class="dark:text-gray-200" id="evictionCost"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">이사비용</span><span class="dark:text-gray-200" id="movingCost"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">수리비 (3%)</span><span class="dark:text-gray-200" id="repairCost"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">법무사 비용</span><span class="dark:text-gray-200" id="lawyerFee"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <div class="text-sm text-gray-600 dark:text-gray-400 space-y-2">
        <p>부동산 경매 신청 시 필요한 경매신청 비용과 감정료, 현황조사수수료 등 예납금을 계산합니다.</p>
      </div>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 비용은 개별 상황에 따라 달라질 수 있습니다.<br>
        ※ 취득세율은 일반적인 경우(4.6%)를 기준으로 계산되었습니다.
      </p>
    </div>
</CalcLayout>

<script>
  const ACQUISITION_TAX_RATE = 0.046
  const EVICTION_COST_DEFAULT = 2000000
  const MOVING_COST_DEFAULT = 500000
  const REPAIR_COST_RATE = 0.03
  const LAWYER_FEE = 500000

  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function calculateAuctionCost(bidPrice) {
    const acquisitionTax = Math.round(bidPrice * ACQUISITION_TAX_RATE)
    const evictionCost = EVICTION_COST_DEFAULT
    const movingCost = MOVING_COST_DEFAULT
    const repairCost = Math.round(bidPrice * REPAIR_COST_RATE)
    const lawyerFee = LAWYER_FEE

    const totalIncidentalCost = acquisitionTax + evictionCost + movingCost + repairCost + lawyerFee
    const totalInvestment = bidPrice + totalIncidentalCost

    return {
      bidPrice,
      acquisitionTax,
      evictionCost,
      movingCost,
      repairCost,
      lawyerFee,
      totalIncidentalCost,
      totalInvestment
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }
  setupNumberInput(document.getElementById('bidPrice'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const raw = document.getElementById('bidPrice').value.replace(/,/g, '')
    const bidPrice = parseInt(raw) || 0

    if (bidPrice <= 0) {
      document.getElementById('errorText').textContent = '낙찰가를 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateAuctionCost(bidPrice)

    document.getElementById('totalInvestment').textContent = formatManWon(result.totalInvestment)
    document.getElementById('totalIncidentalCost').textContent = formatManWon(result.totalIncidentalCost)
    document.getElementById('bidPriceResult').textContent = formatManWon(result.bidPrice)
    document.getElementById('acquisitionTax').textContent = formatManWon(result.acquisitionTax)
    document.getElementById('evictionCost').textContent = formatManWon(result.evictionCost)
    document.getElementById('movingCost').textContent = formatManWon(result.movingCost)
    document.getElementById('repairCost').textContent = formatManWon(result.repairCost)
    document.getElementById('lawyerFee').textContent = formatManWon(result.lawyerFee)

    resultsEl.classList.remove('hidden')
  })
</script>
