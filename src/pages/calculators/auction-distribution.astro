---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="경매 배당 계산기 - 부동산 트렌드 리뷰" description="경매 매각대금의 배당 순위에 따른 배당 금액을 계산하세요. 선순위, 후순위 채권별 배당액과 잔여금을 확인할 수 있습니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">경매 배당 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">경매 매각대금의 배당 순위에 따른 배당금액을 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">매각대금</label>
          <div class="relative">
            <input type="text" id="salePrice" placeholder="500,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">경매비용</label>
          <div class="relative">
            <input type="text" id="auctionCost" placeholder="2,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">선순위채권</label>
          <div class="relative">
            <input type="text" id="seniorClaim" placeholder="200,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">임차보증금</label>
          <div class="relative">
            <input type="text" id="tenantDeposit" placeholder="100,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">후순위채권</label>
          <div class="relative">
            <input type="text" id="juniorClaim" placeholder="50,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">배당 요약</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">총 매각대금</span><span class="font-bold text-blue-700 dark:text-blue-300" id="totalSalePrice"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">총 배당금액</span><span class="font-medium dark:text-gray-200" id="totalDistributed"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">배당 순위별 배당액</h2>
        <div class="space-y-3" id="distributionList"></div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <div class="text-sm text-gray-600 dark:text-gray-400 space-y-2">
        <p>경매 시 배당 순위와 낙찰 금액에 따라 우선배당, 안분배당, 흡수배당되는 금액을 계산합니다.</p>
        <ul class="list-disc list-inside space-y-1 ml-2">
          <li>배당 순위: 1. 경매비용(100~300만원) → 2. 최우선변제 소액보증금, 임금채권 → 3. 당해세(재산세 등) → 4. 압류세금/담보물권(근저당)/우선변제권(임차보증금) → 5. 공과금, 일반채권(가압류) → 6. 소유자 배당</li>
        </ul>
      </div>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 배당은 법원의 배당표에 따라 달라질 수 있습니다.<br>
        ※ 배당 순위는 채권의 종류와 등기 시점에 따라 결정됩니다.
      </p>
    </div>
</CalcLayout>

<script>
  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function calculateAuctionDistribution(salePrice, auctionCost, seniorClaim, tenantDeposit, juniorClaim) {
    let remaining = salePrice
    const distributions = []

    const auctionDist = Math.min(remaining, auctionCost)
    distributions.push({
      name: '경매비용',
      claim: auctionCost,
      distributed: Math.round(auctionDist),
      satisfied: auctionDist >= auctionCost,
      shortfall: Math.round(Math.max(0, auctionCost - auctionDist))
    })
    remaining -= auctionDist

    const seniorDist = Math.min(remaining, seniorClaim)
    distributions.push({
      name: '선순위채권',
      claim: seniorClaim,
      distributed: Math.round(seniorDist),
      satisfied: seniorDist >= seniorClaim,
      shortfall: Math.round(Math.max(0, seniorClaim - seniorDist))
    })
    remaining -= seniorDist

    const tenantDist = Math.min(remaining, tenantDeposit)
    distributions.push({
      name: '임차보증금',
      claim: tenantDeposit,
      distributed: Math.round(tenantDist),
      satisfied: tenantDist >= tenantDeposit,
      shortfall: Math.round(Math.max(0, tenantDeposit - tenantDist))
    })
    remaining -= tenantDist

    const juniorDist = Math.min(remaining, juniorClaim)
    distributions.push({
      name: '후순위채권',
      claim: juniorClaim,
      distributed: Math.round(juniorDist),
      satisfied: juniorDist >= juniorClaim,
      shortfall: Math.round(Math.max(0, juniorClaim - juniorDist))
    })
    remaining -= juniorDist

    distributions.push({
      name: '소유자 잔여금',
      claim: 0,
      distributed: Math.round(remaining),
      satisfied: true,
      shortfall: 0
    })

    const totalDistributed = auctionDist + seniorDist + tenantDist + juniorDist + remaining

    return {
      totalSalePrice: salePrice,
      totalDistributed: Math.round(totalDistributed),
      distributions
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }
  setupNumberInput(document.getElementById('salePrice'))
  setupNumberInput(document.getElementById('auctionCost'))
  setupNumberInput(document.getElementById('seniorClaim'))
  setupNumberInput(document.getElementById('tenantDeposit'))
  setupNumberInput(document.getElementById('juniorClaim'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const salePrice = parseInt(document.getElementById('salePrice').value.replace(/,/g, '')) || 0
    const auctionCost = parseInt(document.getElementById('auctionCost').value.replace(/,/g, '')) || 0
    const seniorClaim = parseInt(document.getElementById('seniorClaim').value.replace(/,/g, '')) || 0
    const tenantDeposit = parseInt(document.getElementById('tenantDeposit').value.replace(/,/g, '')) || 0
    const juniorClaim = parseInt(document.getElementById('juniorClaim').value.replace(/,/g, '')) || 0

    if (salePrice <= 0) {
      document.getElementById('errorText').textContent = '매각대금을 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateAuctionDistribution(salePrice, auctionCost, seniorClaim, tenantDeposit, juniorClaim)

    document.getElementById('totalSalePrice').textContent = formatManWon(result.totalSalePrice)
    document.getElementById('totalDistributed').textContent = formatManWon(result.totalDistributed)

    const distributionList = document.getElementById('distributionList')
    distributionList.innerHTML = ''
    result.distributions.forEach((dist, index) => {
      const div = document.createElement('div')
      div.className = 'border-b border-gray-200 dark:border-gray-700 pb-2 last:border-0'

      const statusColor = dist.satisfied ? 'text-green-600 dark:text-green-400' : 'text-red-600 dark:text-red-400'
      const statusText = dist.satisfied ? '(완전배당)' : '(미완)'

      div.innerHTML = `
        <div class="flex justify-between mb-1">
          <span class="text-sm font-medium dark:text-gray-200">${index + 1}. ${dist.name} <span class="${statusColor} text-xs">${statusText}</span></span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="dark:text-gray-400">채권액</span>
          <span class="dark:text-gray-300">${formatManWon(dist.claim)}</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="dark:text-gray-400">배당액</span>
          <span class="font-medium dark:text-gray-200">${formatManWon(dist.distributed)}</span>
        </div>
        ${dist.shortfall > 0 ? `<div class="flex justify-between text-sm">
          <span class="dark:text-gray-400">부족액</span>
          <span class="text-red-600 dark:text-red-400">${formatManWon(dist.shortfall)}</span>
        </div>` : ''}
      `
      distributionList.appendChild(div)
    })

    resultsEl.classList.remove('hidden')
  })
</script>
