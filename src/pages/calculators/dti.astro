---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="DTI 계산기 - 부동산 트렌드 리뷰" description="DTI(총부채상환비율)을 계산하여 대출 가능 여부를 확인하세요.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">DTI 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">DTI(총부채상환비율)을 계산하여 대출 가능 여부를 확인합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">연소득</label>
          <div class="relative">
            <input type="text" id="annualIncome" placeholder="50,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">신규대출금액</label>
          <div class="relative">
            <input type="text" id="loanAmount" placeholder="300,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">대출기간</label>
          <div class="relative">
            <input type="number" id="loanTermYears" placeholder="30" min="1" max="50"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">년</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">대출금리</label>
          <div class="relative">
            <input type="text" id="loanRate" placeholder="3.5"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">%</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">상환방식</label>
          <select id="repaymentMethod" class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white">
            <option value="원리금균등">원리금균등</option>
            <option value="원금균등">원금균등</option>
            <option value="만기일시">만기일시</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">기존대출 연이자</label>
          <div class="relative">
            <input type="text" id="otherDebtInterest" placeholder="5,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">DTI 계산 결과</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">DTI 비율</span><span class="font-bold text-blue-700 dark:text-blue-300" id="dtiRate"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">월 상환금</span><span class="font-medium dark:text-gray-200" id="monthlyRepay"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">연간 총 상환액</span><span class="font-medium dark:text-gray-200" id="annualTotalRepay"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">세부 내역</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">신규대출 연간상환액</span><span class="dark:text-gray-200" id="loanAnnualRepay"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">기존대출 연이자</span><span class="dark:text-gray-200" id="otherDebtAnnualInterest"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">DTI(Debt to Income: 총부채상환비율)은 연소득 대비 금융비용 부담률을 의미합니다. 소득이 어느정도인 사람이 부담할 수 있는 대출금을 판별하는 지표입니다.</p>
      <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1 list-disc list-inside">
        <li>스트레스DTI는 향후 금리가 오를 것을 감안하여 적용 금리에 일부 가산금리를 더해서 계산합니다.</li>
        <li>상환 방법: 만기일시 / 원금균등분할 / 원리금균등분할</li>
      </ul>
      <p class="text-xs text-gray-500 dark:text-gray-400">※ 은행마다 적용하는 비율이 차이가 날 수 있으며, 실제 대출 시에는 각 금융회사의 내규에 따라 다른 결과값이 나올 수 있습니다.</p>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 대출 조건은 금융기관에 확인하시기 바랍니다.<br>
        ※ DTI는 DSR과 달리 기존대출의 이자만 포함됩니다.
      </p>
    </div>
</CalcLayout>

<script>
  function formatNumber(num) {
    return Math.round(num).toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) {
      r += formatNumber(eok) + '억'
      if (man > 0) r += ' ' + formatNumber(man) + '만'
      if (rem > 0) r += ' ' + formatNumber(rem)
      r += '원'
    } else if (man > 0) {
      r += formatNumber(man) + '만'
      if (rem > 0) r += ' ' + formatNumber(rem)
      r += '원'
    } else {
      r += formatNumber(abs) + '원'
    }
    return r
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }

  function setupDecimalInput(el) {
    el.addEventListener('input', function() {
      let raw = this.value.replace(/[^0-9.]/g, '')
      const parts = raw.split('.')
      if (parts.length > 2) {
        raw = parts[0] + '.' + parts.slice(1).join('')
      }
      this.value = raw
    })
  }

  setupNumberInput(document.getElementById('annualIncome'))
  setupNumberInput(document.getElementById('loanAmount'))
  setupDecimalInput(document.getElementById('loanRate'))
  setupNumberInput(document.getElementById('otherDebtInterest'))

  function calculateMonthlyPayment(loanAmount, loanTermMonths, loanRate, repaymentMethod) {
    const monthlyRate = loanRate / 100 / 12

    if (repaymentMethod === '원리금균등') {
      if (monthlyRate === 0) return loanAmount / loanTermMonths
      const pow = Math.pow(1 + monthlyRate, loanTermMonths)
      return (loanAmount * monthlyRate * pow) / (pow - 1)
    } else if (repaymentMethod === '원금균등') {
      return loanAmount / loanTermMonths + loanAmount * monthlyRate
    } else {
      return loanAmount * monthlyRate
    }
  }

  function calculateDti(input) {
    const monthlyRepay = calculateMonthlyPayment(
      input.loanAmount,
      input.loanTermMonths,
      input.loanRate,
      input.repaymentMethod
    )

    const loanAnnualRepay = Math.round(monthlyRepay * 12)
    const otherDebtAnnualInterest = Math.round(input.otherDebtInterest)
    const annualTotalRepay = loanAnnualRepay + otherDebtAnnualInterest
    const dtiRate = Math.round((annualTotalRepay / input.annualIncome) * 10000) / 100

    return {
      dtiRate,
      annualTotalRepay,
      loanAnnualRepay,
      otherDebtAnnualInterest,
      monthlyRepay: Math.round(monthlyRepay),
    }
  }

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const annualIncome = parseInt(document.getElementById('annualIncome').value.replace(/,/g, '')) || 0
    const loanAmount = parseInt(document.getElementById('loanAmount').value.replace(/,/g, '')) || 0
    const loanTermYears = parseInt(document.getElementById('loanTermYears').value) || 0
    const loanRate = parseFloat(document.getElementById('loanRate').value) || 0
    const repaymentMethod = document.getElementById('repaymentMethod').value
    const otherDebtInterest = parseInt(document.getElementById('otherDebtInterest').value.replace(/,/g, '')) || 0

    if (annualIncome <= 0 || loanAmount < 0 || loanTermYears <= 0 || loanRate < 0) {
      document.getElementById('errorText').textContent = '모든 항목을 올바르게 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateDti({
      annualIncome,
      loanAmount,
      loanTermMonths: loanTermYears * 12,
      loanRate,
      repaymentMethod,
      otherDebtInterest,
    })

    document.getElementById('dtiRate').textContent = result.dtiRate.toFixed(2) + '%'
    document.getElementById('monthlyRepay').textContent = formatManWon(result.monthlyRepay)
    document.getElementById('annualTotalRepay').textContent = formatManWon(result.annualTotalRepay)
    document.getElementById('loanAnnualRepay').textContent = formatManWon(result.loanAnnualRepay)
    document.getElementById('otherDebtAnnualInterest').textContent = formatManWon(result.otherDebtAnnualInterest)

    resultsEl.classList.remove('hidden')
  })
</script>
