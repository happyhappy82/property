---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="착한임대인 세액공제 계산기 - 부동산 트렌드 리뷰" description="월세 인하 시 받을 수 있는 세액공제와 실질 부담 감소액을 계산합니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">착한임대인 세액공제 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">월세 인하 시 받을 수 있는 세제 혜택을 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">기존 월세</label>
          <div class="relative">
            <input type="text" id="previousRent" placeholder="1,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원/월</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">인하된 월세</label>
          <div class="relative">
            <input type="text" id="reducedRent" placeholder="900,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원/월</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">인하 개월 수</label>
          <div class="relative">
            <input type="number" id="monthsReduced" placeholder="12" min="1" max="12"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">개월</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">본인 소득세율</label>
          <div class="relative">
            <input type="number" id="taxRate" placeholder="24" min="6" max="45" step="0.1"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">%</span>
          </div>
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">6% ~ 45% 사이의 본인 소득세율을 입력하세요</p>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">세액공제 혜택</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">세액공제 (70%)</span><span class="font-bold text-blue-700 dark:text-blue-300" id="taxCredit"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">실질 절세액</span><span class="font-bold text-blue-700 dark:text-blue-300" id="actualTaxSavings"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">세부 내역</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">월세 인하액</span><span class="dark:text-gray-200" id="rentReduction"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">총 인하액</span><span class="dark:text-gray-200" id="totalReduction"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">실질 부담 감소</span><span class="dark:text-gray-200" id="netBurdenReduction"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">코로나-19 사태로 인해 일시적으로 시행되는 착한 임대인 제도를 통해 받을 수 있는 세제 혜택과 수익의 변화를 계산합니다.</p>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 세액공제는 관할 세무서에 확인하시기 바랍니다.<br>
        ※ 세액공제율은 총 인하액의 70%입니다.
      </p>
    </div>
</CalcLayout>

<script>
  const TAX_CREDIT_RATE = 0.7

  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function roundToWon(amount) {
    return Math.round(amount)
  }

  function calculateGoodLandlord(previousRent, reducedRent, monthsReduced, taxRate) {
    const rentReduction = roundToWon(previousRent - reducedRent)

    if (rentReduction < 0) {
      throw new Error('인하된 월세가 기존 월세보다 클 수 없습니다.')
    }

    const totalReduction = roundToWon(rentReduction * monthsReduced)
    const taxCredit = roundToWon(totalReduction * TAX_CREDIT_RATE)
    const theoreticalTaxSavings = roundToWon(totalReduction * (taxRate / 100))
    const actualTaxSavings = Math.min(taxCredit, theoreticalTaxSavings)
    const netBurdenReduction = roundToWon(taxCredit - totalReduction + actualTaxSavings)

    return {
      rentReduction,
      totalReduction,
      taxCredit,
      actualTaxSavings,
      netBurdenReduction
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }

  setupNumberInput(document.getElementById('previousRent'))
  setupNumberInput(document.getElementById('reducedRent'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const previousRent = parseInt(document.getElementById('previousRent').value.replace(/,/g, '')) || 0
    const reducedRent = parseInt(document.getElementById('reducedRent').value.replace(/,/g, '')) || 0
    const monthsReduced = parseInt(document.getElementById('monthsReduced').value) || 0
    const taxRate = parseFloat(document.getElementById('taxRate').value) || 0

    if (previousRent <= 0) {
      document.getElementById('errorText').textContent = '기존 월세를 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    if (reducedRent < 0) {
      document.getElementById('errorText').textContent = '인하된 월세를 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    if (monthsReduced <= 0 || monthsReduced > 12) {
      document.getElementById('errorText').textContent = '인하 개월 수는 1~12개월 사이여야 합니다.'
      errorEl.classList.remove('hidden')
      return
    }

    if (taxRate < 6 || taxRate > 45) {
      document.getElementById('errorText').textContent = '세율은 6~45% 사이여야 합니다.'
      errorEl.classList.remove('hidden')
      return
    }

    try {
      const result = calculateGoodLandlord(previousRent, reducedRent, monthsReduced, taxRate)

      document.getElementById('taxCredit').textContent = formatManWon(result.taxCredit)
      document.getElementById('actualTaxSavings').textContent = formatManWon(result.actualTaxSavings)
      document.getElementById('rentReduction').textContent = formatManWon(result.rentReduction)
      document.getElementById('totalReduction').textContent = formatManWon(result.totalReduction)
      document.getElementById('netBurdenReduction').textContent = formatManWon(result.netBurdenReduction)

      resultsEl.classList.remove('hidden')
    } catch (error) {
      document.getElementById('errorText').textContent = error.message
      errorEl.classList.remove('hidden')
    }
  })
</script>
