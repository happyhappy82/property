---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="국민주택채권 계산기 - 부동산 트렌드 리뷰" description="주택 취득 시 구매해야 하는 국민주택채권과 할인 비용을 계산하세요. 지역, 면적, 매매가격을 입력하여 실제 부담액을 확인할 수 있습니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">국민주택채권 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">주택 취득 시 구매해야 하는 국민주택채권과 할인 비용을 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">지역</label>
          <select id="region" class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
            <option value="서울">서울</option>
            <option value="광역시">광역시</option>
            <option value="시지역">시지역</option>
            <option value="기타">기타</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">전용면적</label>
          <select id="areaSize" class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
            <option value="60㎡이하">60㎡ 이하</option>
            <option value="60~85㎡">60~85㎡</option>
            <option value="85㎡초과">85㎡ 초과</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">매매가격</label>
          <div class="relative">
            <input type="text" id="salePrice" placeholder="500,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">실제 부담액</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">실제 부담액</span><span class="font-bold text-blue-700 dark:text-blue-300" id="actualBurden"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">채권 매입률</span><span class="font-medium dark:text-gray-200" id="purchaseRate"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">세부 내역</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">채권 매입금액</span><span class="dark:text-gray-200" id="bondPurchaseAmount"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">할인비용 (약 6%)</span><span class="dark:text-gray-200" id="discountCost"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <div class="text-sm text-gray-600 dark:text-gray-400 space-y-2">
        <p>주택 취득 시 구매해야 되는 국민주택채권, 그 채권을 할인하는데 드는 비용을 계산합니다.</p>
      </div>
      <div class="border border-gray-200 dark:border-gray-600 rounded overflow-hidden">
        <table class="w-full text-xs">
          <thead class="bg-gray-50 dark:bg-gray-700">
            <tr>
              <th class="px-2 py-1.5 text-left dark:text-gray-200">지역</th>
              <th class="px-2 py-1.5 text-center dark:text-gray-200">60㎡ 이하</th>
              <th class="px-2 py-1.5 text-center dark:text-gray-200">60~85㎡</th>
              <th class="px-2 py-1.5 text-center dark:text-gray-200">85㎡ 초과</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200 dark:divide-gray-700">
            <tr><td class="px-2 py-1.5 dark:text-gray-300">서울</td><td class="px-2 py-1.5 text-center dark:text-gray-300">1.3%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">2.1%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">3.1%</td></tr>
            <tr><td class="px-2 py-1.5 dark:text-gray-300">광역시</td><td class="px-2 py-1.5 text-center dark:text-gray-300">0.8%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">1.6%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">2.4%</td></tr>
            <tr><td class="px-2 py-1.5 dark:text-gray-300">시지역</td><td class="px-2 py-1.5 text-center dark:text-gray-300">0.6%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">1.3%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">2.1%</td></tr>
            <tr><td class="px-2 py-1.5 dark:text-gray-300">기타</td><td class="px-2 py-1.5 text-center dark:text-gray-300">0.5%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">1.0%</td><td class="px-2 py-1.5 text-center dark:text-gray-300">1.6%</td></tr>
          </tbody>
        </table>
      </div>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 채권 매입률과 할인율은 시기에 따라 변동될 수 있습니다.<br>
        ※ 할인율은 약 6%를 기준으로 계산되었습니다.
      </p>
    </div>
</CalcLayout>

<script>
  const BOND_RATES = {
    '서울': {
      '60㎡이하': 13 / 1000,
      '60~85㎡': 21 / 1000,
      '85㎡초과': 31 / 1000,
    },
    '광역시': {
      '60㎡이하': 8 / 1000,
      '60~85㎡': 16 / 1000,
      '85㎡초과': 24 / 1000,
    },
    '시지역': {
      '60㎡이하': 6 / 1000,
      '60~85㎡': 13 / 1000,
      '85㎡초과': 21 / 1000,
    },
    '기타': {
      '60㎡이하': 5 / 1000,
      '60~85㎡': 10 / 1000,
      '85㎡초과': 16 / 1000,
    },
  }

  const DISCOUNT_RATE = 0.06

  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function calculateHousingBond(salePrice, region, areaSize) {
    const purchaseRate = BOND_RATES[region][areaSize]
    const bondPurchaseAmount = Math.round(salePrice * purchaseRate)
    const discountCost = Math.round(bondPurchaseAmount * DISCOUNT_RATE)
    const actualBurden = bondPurchaseAmount - discountCost

    return {
      bondPurchaseAmount,
      discountCost,
      actualBurden,
      purchaseRate: Math.round(purchaseRate * 10000) / 10
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }
  setupNumberInput(document.getElementById('salePrice'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const salePrice = parseInt(document.getElementById('salePrice').value.replace(/,/g, '')) || 0
    const region = document.getElementById('region').value
    const areaSize = document.getElementById('areaSize').value

    if (salePrice <= 0) {
      document.getElementById('errorText').textContent = '매매가격을 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateHousingBond(salePrice, region, areaSize)

    document.getElementById('actualBurden').textContent = formatManWon(result.actualBurden)
    document.getElementById('purchaseRate').textContent = result.purchaseRate + '‱ (' + (result.purchaseRate / 10).toFixed(2) + '%)'
    document.getElementById('bondPurchaseAmount').textContent = formatManWon(result.bondPurchaseAmount)
    document.getElementById('discountCost').textContent = formatManWon(result.discountCost)

    resultsEl.classList.remove('hidden')
  })
</script>
