---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="누진세 계산기 - 부동산 트렌드 리뷰" description="누진세 구조의 세금을 간편하게 계산하고 구간별 세액을 확인하세요.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">누진세 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">누진세 구조의 세금을 간편하게 계산하고 구간별 세액을 확인합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">과세표준</label>
          <div class="relative">
            <input type="text" id="taxableIncome" placeholder="100,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">총 세액</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">총 납부세액</span><span class="font-bold text-blue-700 dark:text-blue-300" id="totalTax"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">실효세율</span><span class="font-medium dark:text-gray-200" id="effectiveRate"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">구간별 세액 내역</h2>
        <div id="bracketsList" class="space-y-3"></div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">누진세 구조의 세금을 간편하게 계산하고 속산표를 만들어드리는 계산기입니다.</p>
      <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1 list-disc list-inside">
        <li>누진세(progressive tax)는 소득금액이 커질수록 높은 세율이 적용되는 세금을 말합니다.</li>
        <li>양도세, 상속·증여세, 재산세, 종합부동산세가 모두 누진세입니다. 부가세나 취득세는 누진세가 아닙니다.</li>
        <li>누진공제액은 누진세를 쉽게 계산하도록 하기 위해 나온 개념으로, 과세표준에 해당하는 구간 아래에 있는 각 구간들의 세금을 미리 계산하여 더한 값입니다.</li>
      </ul>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 세금은 관할 세무서에 확인하시기 바랍니다.<br>
        ※ 2026년 소득세 과세표준 기준으로 계산됩니다.
      </p>
    </div>
</CalcLayout>

<script>
  const TAX_BRACKETS = [
    { limit: 14000000, rate: 0.06, name: '1,400만원 이하' },
    { limit: 50000000, rate: 0.15, name: '5,000만원 이하' },
    { limit: 88000000, rate: 0.24, name: '8,800만원 이하' },
    { limit: 150000000, rate: 0.35, name: '1억 5,000만원 이하' },
    { limit: 300000000, rate: 0.38, name: '3억원 이하' },
    { limit: 500000000, rate: 0.4, name: '5억원 이하' },
    { limit: 1000000000, rate: 0.42, name: '10억원 이하' },
    { limit: Infinity, rate: 0.45, name: '10억원 초과' },
  ]

  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function roundToWon(amount) {
    return Math.round(amount)
  }

  function calculateProgressiveTax(taxableIncome) {
    const brackets = []
    let remainingIncome = taxableIncome
    let totalTax = 0
    let previousLimit = 0

    for (const bracket of TAX_BRACKETS) {
      if (remainingIncome <= 0) break

      const bracketLimit = bracket.limit - previousLimit
      const taxableAmount = Math.min(remainingIncome, bracketLimit)
      const taxAmount = roundToWon(taxableAmount * bracket.rate)

      brackets.push({
        bracket: bracket.name,
        taxableAmount,
        rate: bracket.rate * 100,
        taxAmount
      })

      totalTax += taxAmount
      remainingIncome -= taxableAmount
      previousLimit = bracket.limit

      if (bracket.limit === Infinity) break
    }

    const effectiveRate = taxableIncome > 0 ? (totalTax / taxableIncome) * 100 : 0

    return {
      brackets,
      totalTax,
      effectiveRate: Math.round(effectiveRate * 100) / 100
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }

  setupNumberInput(document.getElementById('taxableIncome'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const taxableIncome = parseInt(document.getElementById('taxableIncome').value.replace(/,/g, '')) || 0

    if (taxableIncome < 0) {
      document.getElementById('errorText').textContent = '과세표준을 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateProgressiveTax(taxableIncome)

    document.getElementById('totalTax').textContent = formatManWon(result.totalTax)
    document.getElementById('effectiveRate').textContent = result.effectiveRate + '%'

    const bracketsListEl = document.getElementById('bracketsList')
    bracketsListEl.innerHTML = ''

    result.brackets.forEach((b) => {
      const div = document.createElement('div')
      div.className = 'pb-3 border-b border-gray-200 dark:border-gray-600 last:border-0'
      div.innerHTML = `
        <div class="flex justify-between items-center mb-1">
          <span class="text-sm font-medium dark:text-gray-200">${b.bracket}</span>
          <span class="text-sm font-bold dark:text-gray-200">${formatManWon(b.taxAmount)}</span>
        </div>
        <div class="flex justify-between text-xs text-gray-500 dark:text-gray-400">
          <span>과세금액: ${formatManWon(b.taxableAmount)}</span>
          <span>세율: ${b.rate}%</span>
        </div>
      `
      bracketsListEl.appendChild(div)
    })

    resultsEl.classList.remove('hidden')
  })
</script>
