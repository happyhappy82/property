---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="재건축 연한 계산기 - 부동산 트렌드 리뷰" description="건물 구조와 준공연도를 기반으로 재건축 가능 연한을 계산합니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">재건축 연한 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">건물 구조와 준공연도로 재건축 가능 연도를 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">건물 구조</label>
          <select id="buildingType" class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
            <option value="rc">철근(철골)콘크리트조 (RC/SRC)</option>
            <option value="brick">연와조/조적조</option>
            <option value="wood">목조</option>
            <option value="steel">경량철골조</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">사용승인 연도</label>
          <div class="relative">
            <input type="text" id="approvalYear" placeholder="1990"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">년</span>
          </div>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">재건축 가능 시기</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">표준 재건축 연도</span><span class="font-bold text-blue-700 dark:text-blue-300" id="reconstructionYear"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">조기 재건축 연도</span><span class="font-bold text-blue-700 dark:text-blue-300" id="earlyReconstructionYear"></span></div>
        </div>
      </div>

      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">건물 현황</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">기대 수명</span><span class="dark:text-gray-200" id="standardYears"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">조기 재건축 가능 연한</span><span class="dark:text-gray-200" id="minimumYears"></span></div>
          <div class="flex justify-between border-t border-gray-200 dark:border-gray-700 pt-2"><span class="text-sm dark:text-gray-300">경과 연수</span><span class="dark:text-gray-200" id="elapsedYears"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">잔여 연한</span><span class="font-medium dark:text-gray-200" id="remainingYears"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400">
        도시 및 주거환경정비법과 지역별 조례에 따라 재건축을 실시할 수 있는 노후·불량주택의 연한을 계산합니다.
      </p>
      <div class="overflow-x-auto">
        <table class="w-full text-sm text-gray-600 dark:text-gray-400 border-collapse">
          <thead>
            <tr class="border-b border-gray-200 dark:border-gray-700">
              <th class="text-left py-2 px-2 font-medium">건물 구조</th>
              <th class="text-right py-2 px-2 font-medium">표준연한</th>
              <th class="text-right py-2 px-2 font-medium">최소연한</th>
            </tr>
          </thead>
          <tbody>
            <tr class="border-b border-gray-200 dark:border-gray-700">
              <td class="py-2 px-2">철근(철골)콘크리트조</td>
              <td class="text-right py-2 px-2">40년</td>
              <td class="text-right py-2 px-2">30년</td>
            </tr>
            <tr class="border-b border-gray-200 dark:border-gray-700">
              <td class="py-2 px-2">연와조/조적조</td>
              <td class="text-right py-2 px-2">30년</td>
              <td class="text-right py-2 px-2">20년</td>
            </tr>
            <tr class="border-b border-gray-200 dark:border-gray-700">
              <td class="py-2 px-2">목조</td>
              <td class="text-right py-2 px-2">20년</td>
              <td class="text-right py-2 px-2">15년</td>
            </tr>
            <tr>
              <td class="py-2 px-2">경량철골조</td>
              <td class="text-right py-2 px-2">30년</td>
              <td class="text-right py-2 px-2">20년</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 재건축 가능 여부는 지역 조례와 안전진단을 확인하시기 바랍니다.<br>
        ※ 조기 재건축은 안전진단을 통과한 경우에만 가능합니다.
      </p>
    </div>
</CalcLayout>

<script>
  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      this.value = raw
    })
  }

  setupNumberInput(document.getElementById('approvalYear'))

  const BUILDING_STANDARDS = {
    'rc': { standard: 40, minimum: 30, name: '철근(철골)콘크리트조' },
    'brick': { standard: 30, minimum: 20, name: '연와조/조적조' },
    'wood': { standard: 20, minimum: 15, name: '목조' },
    'steel': { standard: 30, minimum: 20, name: '경량철골조' }
  }

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const buildingType = document.getElementById('buildingType').value
    const approvalYear = parseInt(document.getElementById('approvalYear').value) || 0

    const currentYear = new Date().getFullYear()

    if (approvalYear < 1900) {
      document.getElementById('errorText').textContent = '사용승인일은 1900년 이후여야 합니다.'
      errorEl.classList.remove('hidden')
      return
    }
    if (approvalYear > currentYear) {
      document.getElementById('errorText').textContent = '사용승인일은 현재 연도를 초과할 수 없습니다.'
      errorEl.classList.remove('hidden')
      return
    }

    const elapsedYears = currentYear - approvalYear
    const { standard, minimum } = BUILDING_STANDARDS[buildingType]

    const reconstructionYear = approvalYear + standard
    const earlyReconstructionYear = approvalYear + minimum
    const remainingYears = Math.max(0, standard - elapsedYears)

    document.getElementById('reconstructionYear').textContent = formatNumber(reconstructionYear) + '년'
    document.getElementById('earlyReconstructionYear').textContent = formatNumber(earlyReconstructionYear) + '년'
    document.getElementById('standardYears').textContent = standard + '년'
    document.getElementById('minimumYears').textContent = minimum + '년'
    document.getElementById('elapsedYears').textContent = elapsedYears + '년'
    document.getElementById('remainingYears').textContent = remainingYears + '년'

    resultsEl.classList.remove('hidden')
  })
</script>
