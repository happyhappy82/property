---
import CalcLayout from '../../layouts/CalcLayout.astro'
---

<CalcLayout title="임대소득세 계산기 - 부동산 트렌드 리뷰" description="주택 임대소득세를 간편하게 계산하세요. 분리과세 방식으로 월세 임대소득에 대한 세금을 확인합니다.">
    <div class="mb-6">
      <a href="/calculators" class="text-sm text-blue-600 dark:text-blue-400 hover:underline">&larr; 계산기 목록</a>
    </div>
    <h1 class="text-2xl font-bold mb-2 dark:text-white">임대소득세 계산기</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-sm">주택 임대소득에 대한 분리과세 세금을 계산합니다.</p>

    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-5 mb-6">
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">월세 수입</label>
          <div class="relative">
            <input type="text" id="monthlyRent" placeholder="1,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원/월</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">보증금</label>
          <div class="relative">
            <input type="text" id="deposit" placeholder="10,000,000"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">원</span>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-200">임대 기간</label>
          <div class="relative">
            <input type="number" id="rentalMonths" placeholder="12" min="1"
              class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white pr-8" />
            <span class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm">개월</span>
          </div>
        </div>

        <div class="flex items-center gap-2">
          <input type="checkbox" id="isRegistered" class="rounded" />
          <label for="isRegistered" class="text-sm dark:text-gray-200 cursor-pointer">임대사업자 등록</label>
        </div>

        <button id="calcBtn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2.5 px-4 rounded-lg transition-colors">
          계산하기
        </button>
      </div>
    </div>

    <div id="error" class="hidden bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 p-3 rounded-lg mb-6">
      <p class="text-red-700 dark:text-red-400 text-sm" id="errorText"></p>
    </div>

    <div id="results" class="hidden space-y-4">
      <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg border border-blue-200 dark:border-blue-800 p-4">
        <h2 class="text-sm font-medium text-blue-800 dark:text-blue-300 mb-3">산출세액</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">분리과세 세액</span><span class="font-bold text-blue-700 dark:text-blue-300" id="calculatedTax"></span></div>
        </div>
      </div>
      <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">세부 내역</h2>
        <div class="space-y-2">
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">연간 수입금액</span><span class="dark:text-gray-200" id="annualIncome"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">필요경비</span><span class="dark:text-gray-200" id="expenses"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">기본공제</span><span class="dark:text-gray-200" id="basicDeduction"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">과세표준</span><span class="dark:text-gray-200" id="taxableIncome"></span></div>
          <div class="flex justify-between"><span class="text-sm dark:text-gray-300">필요경비율</span><span class="dark:text-gray-200" id="expenseRate"></span></div>
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4 space-y-4">
      <div class="flex items-center gap-2 text-blue-600 dark:text-blue-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
        <h3 class="text-sm font-semibold">참고 정보</h3>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">주택 임대로 인해 발생하는 소득에 과세되는 세금을 계산합니다. 분리과세는 주택임대 총소득이 2천만원 이하인 경우에만 가능합니다.</p>
      <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1 list-disc list-inside">
        <li>2천만원을 초과하는 경우엔 종합소득세 대상으로 합산하여 과세됩니다.</li>
        <li>입력 수입금액 = 임대 기간 동안의 월세 + 보증금의 간주임대료</li>
      </ul>
      <p class="text-xs text-gray-500 dark:text-gray-400">※ 간주임대료를 정확히 계산하시고자 하는 경우 간주임대료 계산기를 사용하시고 수입금액 직접 입력을 선택하세요.</p>
    </div>

    <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-800/50 rounded-lg border border-gray-200 dark:border-gray-700">
      <p class="text-xs text-gray-500 dark:text-gray-400">
        ※ 본 계산기는 참고용이며, 실제 세금은 관할 세무서에 확인하시기 바랍니다.<br>
        ※ 분리과세 세율은 14%이며, 등록사업자는 필요경비율 60%, 미등록은 50%가 적용됩니다.
      </p>
    </div>
</CalcLayout>

<script>
  const SEPARATE_TAX_RATE = 0.14
  const REGISTERED_EXPENSE_RATE = 0.6
  const UNREGISTERED_EXPENSE_RATE = 0.5
  const REGISTERED_BASIC_DEDUCTION = 4000000
  const UNREGISTERED_BASIC_DEDUCTION = 2000000

  function formatNumber(num) {
    return num.toLocaleString('ko-KR')
  }

  function formatManWon(amount) {
    const abs = Math.abs(Math.round(amount))
    const sign = amount < 0 ? '-' : ''
    const eok = Math.floor(abs / 100000000)
    const man = Math.floor((abs % 100000000) / 10000)
    const rem = abs % 10000
    let r = sign
    if (eok > 0) { r += formatNumber(eok) + '억'; if (man > 0) r += ' ' + formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else if (man > 0) { r += formatNumber(man) + '만'; if (rem > 0) r += ' ' + formatNumber(rem); r += '원' }
    else { r += formatNumber(abs) + '원' }
    return r
  }

  function roundToWon(amount) {
    return Math.round(amount)
  }

  function calculateRentalIncomeTax(monthlyRent, rentalMonths, isRegistered) {
    const annualIncome = roundToWon((monthlyRent * rentalMonths * 12) / rentalMonths)
    const expenseRate = isRegistered ? REGISTERED_EXPENSE_RATE : UNREGISTERED_EXPENSE_RATE
    const expenses = roundToWon(annualIncome * expenseRate)
    const basicDeduction = isRegistered ? REGISTERED_BASIC_DEDUCTION : UNREGISTERED_BASIC_DEDUCTION
    const taxableIncome = Math.max(0, annualIncome - expenses - basicDeduction)
    const calculatedTax = roundToWon(taxableIncome * SEPARATE_TAX_RATE)

    return {
      annualIncome,
      expenses,
      basicDeduction,
      taxableIncome,
      calculatedTax,
      expenseRate: expenseRate * 100
    }
  }

  function setupNumberInput(el) {
    el.addEventListener('input', function() {
      const raw = this.value.replace(/[^0-9]/g, '')
      if (raw) { this.value = parseInt(raw).toLocaleString('ko-KR') }
      else { this.value = '' }
    })
  }

  setupNumberInput(document.getElementById('monthlyRent'))
  setupNumberInput(document.getElementById('deposit'))

  document.getElementById('calcBtn').addEventListener('click', () => {
    const errorEl = document.getElementById('error')
    const resultsEl = document.getElementById('results')
    errorEl.classList.add('hidden')
    resultsEl.classList.add('hidden')

    const monthlyRent = parseInt(document.getElementById('monthlyRent').value.replace(/,/g, '')) || 0
    const rentalMonths = parseInt(document.getElementById('rentalMonths').value) || 12
    const isRegistered = document.getElementById('isRegistered').checked

    if (monthlyRent <= 0) {
      document.getElementById('errorText').textContent = '월세 수입을 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    if (rentalMonths <= 0) {
      document.getElementById('errorText').textContent = '임대 기간을 입력해주세요.'
      errorEl.classList.remove('hidden')
      return
    }

    const result = calculateRentalIncomeTax(monthlyRent, rentalMonths, isRegistered)

    document.getElementById('calculatedTax').textContent = formatManWon(result.calculatedTax)
    document.getElementById('annualIncome').textContent = formatManWon(result.annualIncome)
    document.getElementById('expenses').textContent = formatManWon(result.expenses)
    document.getElementById('basicDeduction').textContent = formatManWon(result.basicDeduction)
    document.getElementById('taxableIncome').textContent = formatManWon(result.taxableIncome)
    document.getElementById('expenseRate').textContent = result.expenseRate + '%'

    resultsEl.classList.remove('hidden')
  })
</script>
